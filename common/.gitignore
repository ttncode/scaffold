node_modules/
dist/
build/
coverage/

# .env* rather than .env: install.sh writes ./.env.XXXXXX holding the
# generated database password, and a signal between creating and moving it
# leaves that file behind.
.env*
!.env.example
*.local

.DS_Store
*.swp
