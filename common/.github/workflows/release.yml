name: Release
on:
  push:
    branches: [main]
permissions: {}
jobs:
  release:
    uses: you/.github/.github/workflows/app-release.yml@v1
    with:
      image: ghcr.io/you/@PROJECT_NAME@
      context: apps/api
