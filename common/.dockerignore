# only read when the build context is the project root — an all-typescript
# project's apps/*/Dockerfile.workspace; node_modules is copied from a pinned
# build stage, so a host copy would overwrite it
**/node_modules
**/.next
**/dist
**/coverage

**/.env
**/.env.*
!**/.env.example
.git
