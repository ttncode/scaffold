# copy to .env and edit. install.sh does this for you and generates a random
# value for every password below in place of the literal `changeme`. the
# per-application port variables, and the database and cache variables, are
# appended by scaffold from whatever the project was generated with.
IMAGE_TAG=latest
