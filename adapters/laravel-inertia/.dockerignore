# vendor is copied from a pinned build stage
vendor
node_modules

.env
.env.*
!.env.example
.git
storage/logs
storage/framework/cache

# the manifests, not the directory: local ones name dev providers a --no-dev
# image lacks, and artisan will not boot without the directory
bootstrap/cache/*.php

# a host `npm run build` would overwrite the assets stage's output
public/build
