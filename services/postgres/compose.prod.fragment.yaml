services:
  database:
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - database:/var/lib/postgresql/data
    restart: always
volumes:
  # {}, not a bare key: a YAML null merges into service.bats' "!= null"
  # check as null, and compose treats the two identically anyway.
  database: {}
