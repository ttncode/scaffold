## What this changes

<!-- What behaviour is different after this, and why it needed to be. -->

## How it was verified

<!-- The commands you ran and what they said. `mise run test-runner` covers the
     environment CI actually has; a green `test-unit` alone does not. -->

## Checklist

- [ ] `mise run lint` passes
- [ ] `mise run test-runner` passes
- [ ] New behaviour has a test that fails without the change
- [ ] Docs that describe changed behaviour were updated in the same commit
- [ ] No unrelated changes
