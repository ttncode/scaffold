# Minimal, and real enough for the one thing this fixture is asked to prove:
# cmd_add registers a build target, and register_image_target refuses a target
# whose Dockerfile is not there. Nothing builds this.
FROM scratch
# @SERVICE_SETUP@
EXPOSE 8080
